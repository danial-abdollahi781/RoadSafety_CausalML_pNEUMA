من دانیال عبدالهی هستم و قصد دارم یک مقاله علمی با کیفیت بالا برای چاپ در ژورنال‌های معتبر (Q1 یا Q2) بنویسم.
موضوع پژوهش من: «ارزیابی علّی اثر موتورسیکلت‌ها بر شدت شبه‌تصادفات ترافیکی با استفاده از یادگیری ماشین علّی (Causal ML) و داده‌های مسیر حرکت پهپادی».

داده‌های پژوهش:
ما از دیتاست اوپن‌سورس pNEUMA (دانشگاه EPFL) استفاده می‌کنیم. داده‌ها شامل مختصات جغرافیایی (Lat/Lon)، سرعت و شتاب وسایل نقلیه در هر 0.04 ثانیه است. این فایل‌ها به شدت حجیم (Wide Format) هستند.

روش‌شناسی (Methodology) که باید در زبان R پیاده‌سازی کنی:

تبدیل داده‌های خام به Tidy Data با استفاده از پکیج data.table.

استخراج فاصله فیزیکی وسایل نقلیه با فرمول Haversine.

محاسبه شاخص زمان تا برخورد (TTC) برای شناسایی تعاملات پرخطر.

حذف خطاهای ردیابی پهپاد (فواصل فیزیکی زیر 1.5 متر).

تجمیع فریم‌های متوالی به رویدادهای مستقل (Unique Conflict Events).

پیاده‌سازی تکنیک Propensity Score Matching (PSM) با پکیج MatchIt برای همسان‌سازی سرعت و فواصل، و اثبات اثر علّی موتورسیکلت‌ها بر کاهش TTC.

دستورالعمل خروجی‌ها (Output Requirements):

تو به عنوان برنامه‌نویس ارشد، باید کدها را به صورت تمیز و ماژولار بنویسی و در اکانت گیت‌هاب من قرار دهی.

متن نهایی مقاله باید از نظر آکادمیک بی‌نقص باشد. جملات باید کاملاً انسانی‌سازی شوند و از به کار بردن عبارات کلیشه‌ای هوش مصنوعی خودداری شود.

مقاله نهایی را دقیقاً در سه فرمت به من تحویل بده: فایل Word (به زبان فارسی)، فایل PDF (به زبان انگلیسی) و فایل HTML (به زبان انگلیسی).

در صفحه اول ریپازیتوری (فایل README.md)، یک جدول زیبا و منظم طراحی کن که لینک دانلود مستقیم این سه فایل در آن قرار داشته باشد.

اصلاحیات:

با سلام و احترام،
با توجه به اینکه برای توسعه و اصلاح کدهای این پروژه از عامل هوش مصنوعی کلود (Claude in VS Code) استفاده می‌کنید، دستورالعمل‌های اصلاحی را با جزئیات دقیق فنی، متدولوژیک و ساختار کدنویسی برای شما آماده کرده‌ام.
این توضیحات را به عنوان Prompt (دستور کار) به کلود بدهید تا کلاس‌ها، توابع و فرآیندهای تحلیل داده را دقیقاً بر اساس این متدولوژی جدید بازنویسی کند. تمام مراحل زیر باید به صورت گام‌به‌گام و بدون حذف هیچ موردی در پروژه پیاده‌سازی شوند.
بخش ۱: اصلاح پایگاه داده، فیلترینگ و ابعاد داده‌ها (Data Preparation & Filtering)
مشکل فعلی: نمونه‌گیری ۵۰,۰۰۰ ردیفی بسیار کوچک است و آستانه ۳ فریم تعامل ترافیکی (۰.۱۲ ثانیه) نویز بسیار بالایی وارد مدل می‌کند.
دستورالعمل اجرا برای کلود:
افزایش حجم داده: اگر کدها در R نوشته شده‌اند، از پکیج data.table و اگر در پایتون هستند، از کتابخانه‌های سریع مانند polars یا pandas با فعال‌سازی پردازش موازی استفاده کنید. حجم کل داده‌های خوانده‌شده از پلتفرم pNEUMA را حداقل به ۱,۰۰۰,۰۰۰ سطر یا حداقل ۱۵,۰۰۰ شناسه خودروی منحصر‌به‌فرد افزایش دهید.
فیلتر کردن تعاملات کوتاه‌مدت (Rolling Window): کد را طوری تغییر دهید که ابتدا بر اساس شناسه تعامل (interaction_id) گروه‌بندی (group_by) انجام شود. سپس، تنها تعاملاتی حفظ شوند که در آن‌ها شرط فاصله کمتر از ۳۰ متر و TTC کمتر از ۳ ثانیه، به صورت مستمر حداقل برای ۲۵ فریم متوالی (معادل ۱ ثانیه) برقرار باشد.
نمونه کد پیشنهادی برای اعمال فیلتر (قابل ارائه به کلود):
# نمونه کد پایتون برای فیلترینگ تعاملات مستمر (حداقل 25 فریم)
df = df.sort_values(by=['interaction_id', 'frame_id'])
df['is_valid_interaction'] = (df['distance'] <= 30) & (df['ttc'] <= 3)

# محاسبه طول توالی‌های متوالی برقرار بودن شرط
df['consecutive_valid'] = df.groupby('interaction_id')['is_valid_interaction'].transform(
    lambda x: x.groupby((~x).cumsum()).cumsum()
)
valid_interactions = df[df['consecutive_valid'] >= 25]['interaction_id'].unique()
filtered_df = df[df['interaction_id'].isin(valid_interactions)]
بخش ۲: تغییر فرمول فاصله از هورسین به اقلیدسی (Euclidean Coordinate Calculation)
مشکل فعلی: استفاده از فرمول هورسین در ابعاد کوچک جاده‌ای نادرست است و بار محاسباتی بیهوده دارد.
دستورالعمل اجرا برای کلود:
فرمول هورسین (Haversine) را به طور کامل از اسکریپت حذف کنید.
از آنجا که داده‌های pNEUMA از قبل روی سیستم مختصات کارتزین تصویر شده‌اند (UTM)، فاصله فیزیکی بین خودروی i و j در فریم tرا با فرمول استاندارد فاصله اقلیدسی ۲ بعدی محاسبه کنید:
این کار سرعت اجرای کدهای شما را تا ۱۰ برابر افزایش می‌دهد و دقت میلی‌متری فواصل را در محدوده ۳۰ متری تعاملات حفظ می‌کند.
بخش ۳: پیاده‌سازی شاخص زمان تا تصادف دوبعدی (2D TTC / Modified TTC)
مشکل فعلی: محاسبات یک‌بعدی فعلی برای موتورسیکلت‌ها که مدام تغییر مسیر عرضی (Lane-splitting) دارند، خطا دارد.
دستورالعمل اجرا برای کلود:
فرمول کلاسیک یک‌بعدی را حذف کنید و فرمول سرعت نسبی برداری را قرار دهید.
اگر بردار موقعیت نسبی دو خودرو به صورت 
r
⃗
=
x
⃗
j
−
x
⃗
i
r
=xj
 
​−
x
i
 ​ 
و
 ب
ر
د
ا
ر سرعت نسبی به صورت v⃗=v⃗i−
v
⃗
j
v
=
v
i
 
​
−
v
j ​
 
با
ش
د
، ف
ر
مو
ل
 
د
وبعدی TTC را پیاده‌سازی کنید:TTC2D=−r⃗⋅v⃗∥v
⃗
∥
2
T
T
C
2
D
 
​
=
−
∥
v
∥
2
r
⋅v ​
شر
ط
 همگ
را
ی
ی ر
ا
 
ا
عما
ل
 
ک
ن
ید: این فرمول تنها زمانی معتبر است که ضرب داخلی r⃗⋅v⃗>0r⋅v>0 باشد (یعنی
 
د
و
 
و
س
ی
ل
ه ن
ق
لیه
 در حال نزدیک شدن به یکدیگر باشند). در غیر این صورت، مقدار TTC را بی‌نهایت یا تعریف‌نشده (NaN) در نظر بگیرید.
 بخش ۴: پیاده‌سازی یادگیری ماشین علّی پیشرفته (Causal Forest & Double Machine Learning)
مشکل فعلی: استفاده از رگرسیون لجستیک ساده برای Propensity Score در حد استانداردهای تراز اول علمی پژوهشی نیست.
دستورالعمل اجرا برای کلود:
استفاده از کتابخانه‌های تخصصی علّی:
اگر در پایتون کدنویسی می‌کنید، به کلود بگویید از کتابخانه EconML یا DoWhy استفاده کند.
اگر در R هستید، کلود باید از پکیج grf (توابع Generalized Random Forests) استفاده کند.
پیاده‌سازی الگوریتم جنگل علّی (Causal Forest):
متغیر تیمار (
Z
Z
): حضور حداقل یک موتورسیکلت در تعامل (با مقدار ۱) و خودروهای سواری خالص (با مقدار ۰).
متغیر پاسخ (
Y
Y
): مقدار حداقل 
T
T
C
2
D
TTC2
D 
​
 
در طول تعامل.م
تغیرهای کمکی/مخدوش‌کننده (X
X
)
: فاصله اولیه، میانگین سرعت نسبی، مدت زمان تعامل، و نرخ شتاب تعامل.ن
مونه فرآیند کدنویسی جنگل علّی در R (جهت استفاده کلود):c
odeR
l
ibrary(grf)#
 تعریف متغیرهاX
 <- as.matrix(df[, c("initial_distance", "mean_rel_speed", "duration_frames")])Y
 <- df$min_ttcW
 <- df$treatment # 1 if PTW present, 0 otherwise#

 آموزش جنگل علّیc
_forest <- causal_forest(X, Y, W, num.trees = 2000)#

 برآورد اثر متوسط تیمار (ATE)a
te <- average_treatment_effect(c_forest, target.sample = "all")p
rint(ate)
بخش ۵: تحلیل اثرهای ناهمگن تیمار (CATE) و استفاده از SHAP
مشکل فعلی: نتایج پروژه در حال حاضر فقط یک عدد ثابت کاهش میانگین را نشان می‌دهد که عمق تحلیل پایینی دارد.
دستورالعمل اجرا برای کلود:
بعد از اجرای جنگل علّی، اثر متمایز تیمار برای هر نمونه داده (Conditional Average Treatment Effect - CATE) را پیش‌بینی کنید.
از کتابخانه shap در پایتون استفاده کنید تا مشخص شود کدام یک از متغیرهای مخدوش‌کننده (مانند سرعت نسبی یا فاصله اولیه) بیشترین تاثیر را بر روی تفاوت در میزان اثرگذاری موتورسیکلت‌ها بر کاهش TTC دارند.
نمودار توزیع اثر تیمار (CATE Distribution Plot) را رسم کنید تا توزیع آماری اثرات علّیِ فردی در کل نمونه‌ها به تصویر کشیده شود.
بخش ۶: اجرای آزمون حساسیت (Rosenbaum Bounds Sensitivity Analysis)
مشکل فعلی: اثبات مقاومت مدل در برابر متغیرهای اندازه‌گیری‌نشده (مانند شرایط روانی راننده، آب و هوا و غیره) انجام نشده است.
دستورالعمل اجرا برای کلود:
به کلود دستور دهید که بعد از انجام تطبیق همسایگی (Nearest Neighbor Matching)، آزمون حساسیت مرزهای رزنبام را بر روی زوج‌های جفت‌شده اجرا کند.
در R، از پکیج rbounds و تابع psens برای خروجی‌های پیوسته استفاده کنید.
مقدار آستانه گاما (
Γ
Γ
) را محاسبه کنید؛ یعنی مشخص کنید مخدوش‌کننده‌های پنهان چقدر باید قوی باشند تا اثر علّی موتورسیکلت بر کاهش TTC از نظر آماری غیرمعنی‌دار شود. این آستانه عددی باید گزارش شود.